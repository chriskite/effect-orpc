---
"@chriskite/effect-orpc": patch
---

Docs-only patch: republish so the npm-shipped README includes the "Effect
Schema in `.input()` / `.output()`" section that landed alongside the 0.6.0
feature. The 0.6.0 tarball was assembled against an unsynced root README and
shipped without the new docs; no runtime behavior changes.
